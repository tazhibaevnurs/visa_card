{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "VERCEL=1 python manage.py collectstatic --noinput",
  "rewrites": [
    { "source": "/static/(.*)", "destination": "/static/$1" },
    { "source": "/(.*)", "destination": "/api/wsgi" }
  ]
}
